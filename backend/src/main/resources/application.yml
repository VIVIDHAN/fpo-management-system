spring:
  datasource:
    # H2 In-Memory Database for zero-config local running on Mac / Dev:
    url: jdbc:h2:mem:agricoopdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: password

    # MySQL Configuration (Uncomment if running local MySQL server on Mac/EC2):
    # url: jdbc:mysql://localhost:3306/agricoop?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    # username: root
    # password: Root@12345
    # driver-class-name: com.mysql.cj.jdbc.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: vividhan007@gmail.com
    password: jtdddorgpfxkhwqa
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

server:
  port: 8080
